[
  {
    "name": "log_roll_size_mb",
    "type": "int",
    "defaultValue": "1024",
    "comment": "The max size of one sys log and audit log",
    "isMutable": "false",
    "scope": "FE",
    "define": "/home/seaven/documents/workspace/starrocks/fe/fe-core/src/main/java/com/starrocks/common/Config.java:57",
    "useLocations": [],
    "documents": {
      "en": "##### log_roll_size_mb\n\n- Default: 1024\n- Type: Int\n- Unit: MB\n- Is mutable: No\n- Description: Controls the maximum size of individual system log and audit log files before rotation occurs. When a log file reaches this size threshold, StarRocks automatically creates a new log file to continue logging. This prevents individual log files from growing indefinitely and helps manage disk space usage. Since this is an immutable configuration, the value must be set appropriately during initial deployment based on expected log volume and available disk space.\n- Introduced in: -",
      "zh": "##### log_roll_size_mb\n\n- 默认值：1024\n- 类型：Int\n- 单位：MB\n- 是否可变：否\n- 描述：控制系统日志和审计日志文件在轮转前的最大大小。当日志文件达到该大小阈值时，StarRocks 会自动创建新的日志文件以继续记录。这可防止单个日志文件无限增长，并有助于管理磁盘空间占用。由于该配置不可变，必须在初始部署时根据预期日志量和可用磁盘空间合理设置。\n- 引入版本：-",
      "ja": "##### log_roll_size_mb\n\n- デフォルト: 1024\n- タイプ: Int\n- 単位: MB\n- 変更可能: No\n- 説明: ローテーションが発生する前の個別のシステムログおよび監査ログファイルの最大サイズを制御します。ログファイルがこのサイズ閾値に達すると、StarRocks は新しいログファイルを自動的に作成してログ記録を継続します。これにより、個別のログファイルが無限に増加することを防ぎ、ディスク容量の使用を管理するのに役立ちます。この設定は変更不可のため、予想されるログボリュームと利用可能なディスク容量に基づいて、初期デプロイ時に適切な値を設定する必要があります。\n- 導入バージョン: -"
    },
    "catalog": "Logging"
  },
  {
    "name": "sys_log_dir",
    "type": "String",
    "defaultValue": "Config.STARROCKS_HOME_DIR + \"/log\"",
    "comment": "sys_log_dir: This specifies FE log dir. FE will produce 2 kinds of log files: fe.log:      all logs of FE process. fe.warn.log  all WARNING and ERROR log of FE process. <p> sys_log_level: INFO, WARNING, ERROR, FATAL <p> sys_log_roll_num: Maximal FE log files to be kept within a sys_log_roll_interval. default is 10, which means there will be at most 10 log files in a day <p> sys_log_verbose_modules: Verbose modules. VERBOSE level is implemented by log4j DEBUG level. eg: sys_log_verbose_modules = com.starrocks.globalStateMgr This will only print debug log of files in package com.starrocks.globalStateMgr and all its sub packages. <p> sys_log_roll_interval: DAY:  log suffix is yyyyMMdd HOUR: log suffix is yyyyMMddHH <p> sys_log_delete_age: default is 7 days, if log's last modify time is 7 days ago, it will be deleted. support format: 7d      7 days 10h     10 hours 60m     60 mins 120s    120 seconds <p> sys_log_enable_compress: default is false. if true, then compress fe.log & fe.warn.log by gzip",
    "isMutable": "false",
    "scope": "FE",
    "define": "/home/seaven/documents/workspace/starrocks/fe/fe-core/src/main/java/com/starrocks/common/Config.java:94",
    "useLocations": [],
    "documents": {
      "en": "##### sys_log_dir\n\n- Default: ${STARROCKS_HOME}/log\n- Type: String\n- Unit: N/A\n- Is mutable: No\n- Description: Absolute path to the directory where Frontend (FE) log files are stored. The directory must exist and be writable by the FE process. Two log files are created here: `fe.log` (all FE logs) and `fe.warn.log` (only WARNING and ERROR logs). If the path is relative, it is resolved against the FE working directory. Changing this requires a full FE restart.\n- Introduced in: -",
      "zh": "##### sys_log_dir\n\n- 默认值：${STARROCKS_HOME}/log\n- 类型：String\n- 单位：N/A\n- 是否可变：否\n- 描述：Frontend（FE）日志文件存放目录的绝对路径。该目录必须存在且 FE 进程对其有写权限。系统会在此创建两个日志文件：`fe.log`（记录所有 FE 日志）和 `fe.warn.log`（仅记录 WARNING 与 ERROR 日志）。若路径为相对路径，则相对于 FE 工作目录解析。修改此参数需完整重启 FE。\n- 引入版本：-",
      "ja": "##### sys_log_dir\n\n- デフォルト: ${STARROCKS_HOME}/log\n- タイプ: String\n- 単位: N/A\n- 可変: いいえ\n- 説明: Frontend (FE) のログファイルを格納するディレクトリの絶対パス。このディレクトリは存在し、FEプロセスから書き込み可能である必要があります。ここには `fe.log`（すべてのFEログ）と `fe.warn.log`（WARNINGおよびERRORログのみ）の2つのログファイルが作成されます。パスが相対の場合、FEの作業ディレクトリから解決されます。この設定を変更するにはFEの完全な再起動が必要です。\n- 導入バージョン: -"
    },
    "catalog": "Logging"
  }
]